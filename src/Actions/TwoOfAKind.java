package Actions;

public class TwoOfAKind {
    // if cards in hand match each other than a counter will say how many matching

    //Playing matching Pairs of Cat Cards (where you
    //get to steal a random card from another player)
    //no longer only applies to pairs of Cat Cards. It
    //now applies to ANY pair of cards with the same
    //title (a pair of Shuffle Cards, a pair of Attack
    //Cards, etc). Ignore the instructions on the cards
    //when you play them as a Special Combo
}
